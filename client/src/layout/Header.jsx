import 'Header.css'
import { NavLinks } from './NavLinks'

export const Header = () => {
    return (
        <header>
            <nav>
                <NavLinks />
            </nav>
        </header>
    )
}